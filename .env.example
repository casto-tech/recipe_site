# ── Django Core ──────────────────────────────────────────────────────────────
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
DJANGO_SECRET_KEY=romO*lT?DRLf7=g0frlnuC&aCR!+ospoP_Ewrl&9Plqi*@Us-t

DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_SETTINGS_MODULE=config.settings.development

# Controls whether the Django admin is mounted at /management/
# True in local dev only — always False in production
ADMIN_ENABLED=True

# ── Database ─────────────────────────────────────────────────────────────────
DB_NAME=recipes
DB_USER=recipes_user

# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
DB_PASSWORD=fik#dora+ti&Ubru=hLtrubrupib&frew_pR#8TIvozAwAtHeM

DB_HOST=db
DB_PORT=5432

# ── Azure Blob Storage ───────────────────────────────────────────────────────
# Leave all three BLANK in local dev — Django will use the local media/ folder.
# In production these are set via Azure Key Vault / Container App env vars.
AZURE_STORAGE_ACCOUNT_NAME=
AZURE_STORAGE_ACCOUNT_KEY=
AZURE_STORAGE_CONTAINER=media
